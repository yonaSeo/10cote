#include <bits/stdc++.h>
using namespace std;

int main(void) {
    vector<int> v = {1, 2, 3};
    for (int e : v) cout << e << ' ';
    cout << '\n';
    for (int i = 0; i < v.size(); i++) cout << v[i] << ' ';
    cout << '\n';
    return 0;
}
